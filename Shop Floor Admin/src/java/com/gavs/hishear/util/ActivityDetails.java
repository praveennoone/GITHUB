/**
 * 
 */
package com.gavs.hishear.util;

/**
 * @author balajij
 * 
 */
public class ActivityDetails {

	/*
	 * public Map activitiesTime(String empid) { ManagementDao dao = new
	 * ManagementDaoImpl(); ManagementalDto dto; Map activityTimes = new
	 * HashMap(); ArrayList activity = new ArrayList(); ArrayList actTime = new
	 * ArrayList(); float totalTime = 0.0f; ArrayList activityDetails =
	 * dao.getActivityNo(empid); if(activityDetails != null &&
	 * activityDetails.size() != 0) { for(int act = 0; act <
	 * activityDetails.size(); act++) { dto = (ManagementalDto)
	 * activityDetails.get(act); String tempTime =
	 * dao.getActivityTime(dto.getActivityNo());
	 * activity.add(dto.getActivity()); actTime.add(tempTime); totalTime =
	 * totalTime + Float.parseFloat(tempTime); } activityTimes.put("activity",
	 * activity); activityTimes.put("activityTime", actTime);
	 * activityTimes.put("totalTime", totalTime); }
	 * 
	 * ArrayList activities = dao.getActivities(); if(activities != null) {
	 * 
	 * for(int i=0; i< activities.size(); i++) { } } //return activityTimes; }
	 */
}